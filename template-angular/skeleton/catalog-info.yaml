apiVersion: backstage.io/v1alpha1
kind: Component

metadata:
  name: ${{ values.name  }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: udhayakiran-stack/${{ values.name  }}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: production
  owner: ${{ values.owner }}